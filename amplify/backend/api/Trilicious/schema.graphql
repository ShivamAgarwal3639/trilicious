type User @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
  number: String
  email: String
  dob: String
  date: AWSDateTime
  block: Boolean
  avatar: String
  long: Float
  lat: Float
}

type UntitledModel @model @auth(rules: [{allow: public}]) {
  id: ID!
}
