type UserData @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
  number: String
  email: String
  dob: String
  date: AWSDateTime
  block: Boolean
  avatar: String
  long: Float
  lat: Float
  authId: String!
}

type UntitledModel @model @auth(rules: [{allow: public}]) {
  id: ID!
}
 